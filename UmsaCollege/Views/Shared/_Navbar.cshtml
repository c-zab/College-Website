<nav class="navbar navbar-light navbar-expand-md bg-primary justify-content-center vertical-center" style="margin-bottom: 1rem">

    <button class="navbar-toggler vertical-center" type="button" data-toggle="collapse" data-target="#collapsingNavbar">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="navbar-collapse collapse justify-content-between align-items-center navbar-font-size" id="collapsingNavbar">
        <ul class="header navbar-nav mx-auto text-center vertical-center">

            <li class="nav-item" style="margin-right: 1em">
                <p class="header-text">
                    <a class="nav-item nav-link" asp-controller="Home"
                       asp-action="Index">
                        <img src="~/images/logoCollegemin4.png" width="130" height="50" class="d-inline-block align-top" alt="" asp-append-version="true">
                    </a>
                </p>
            </li>
            <li class="nav-item" style="margin-right: 1em">
                <p class="header-text">
                    <a class="@if( ViewBag.Title == "Display") { @ViewBag.Active } header nav-item nav-link" asp-controller="Home"
                       asp-action="DisplayPage">Display</a>
                </p>
            </li>
            <li class="nav-item" style="margin-right: 1em">
                <p class="header-text">
                    <a class="@if( ViewBag.Title == "Insert") { @ViewBag.Active } header nav-item nav-link" asp-controller="Home"
                       asp-action="InsertPage">Insert</a>
                </p>
            </li>
            <li class="nav-item" style="margin-right: 1em">
                <p class="header-text">
                    <a class="@if( ViewBag.Title == "Data") { @ViewBag.Active } header nav-item nav-link" asp-controller="Home"
                       asp-action="DataPage">Data</a>
                </p>
            </li>
            <li class="nav-item" style="margin-right: 1em">
                <p class="header-text">
                    <a class="@if( ViewBag.Title == "User") { @ViewBag.Active } header nav-item nav-link" asp-controller="Home"
                       asp-action="UserPage">User</a>
                </p>
            </li>
            <li>
                @if (User?.Identity?.IsAuthenticated ?? false) {
                    <a asp-controller="Account" asp-action="Logout"
                       class="btn btn-danger">Logout</a>
                }
            </li>  
        </ul>
    </div>
</nav>